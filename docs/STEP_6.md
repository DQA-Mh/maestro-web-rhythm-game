# Phase 6, beatmap

Added a JSON beatmap loader with a bundled Moonlit Overture demo beatmap and an offline MIDI conversion script.

Run `python tools/midi_to_beatmap.py input.mid output.json` after installing `mido`.